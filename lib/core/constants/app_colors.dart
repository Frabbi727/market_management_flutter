import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFF1976D2); // Blue
  static const Color primaryLight = Color(0xFF63A4FF);
  static const Color primaryDark = Color(0xFF004BA0);

  static const Color secondary = Color(0xFF0288D1); // Light Blue
  static const Color secondaryLight = Color(0xFF5EB8FF);
  static const Color secondaryDark = Color(0xFF005B9F);

  static const Color accent = Color(0xFFFF6F00); // Orange

  // Background Colors
  static const Color background = Color(0xFFF5F7FA);
  static const Color surface = Colors.white;
  static const Color surfaceDark = Color(0xFFF5F5F5);

  // KPI Card Colors
  static const Color electricity = Color(0xFFFF9800); // Orange
  static const Color amount = Color(0xFF1976D2); // Blue
  static const Color acCost = Color(0xFF00ACC1); // Cyan
  static const Color serviceCost = Color(0xFF43A047); // Green
  static const Color invoice = Color(0xFF8E24AA); // Purple
  static const Color status = Color(0xFF00897B); // Teal

  // Text Colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFF9E9E9E);
  static const Color textWhite = Colors.white;

  // Semantic Colors
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFA726);
  static const Color error = Color(0xFFF44336);
  static const Color info = Color(0xFF2196F3);

  // Border Colors
  static const Color border = Color(0xFFE0E0E0);
  static const Color divider = Color(0xFFBDBDBD);

  // Shadow Colors
  static const Color shadow = Color(0x1A000000);
  static const Color shadowLight = Color(0x0D000000);
}