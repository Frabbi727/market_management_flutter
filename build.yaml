targets:
  $default:
    builders:
      riverpod_generator:
        generate_for:
          - lib/**/*.dart
      json_serializable:
        options:
          explicit_to_json: true
          field_rename: snake_case